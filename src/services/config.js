const config = {
    url: {
        baseURL: 'https://api-scrapbook.herokuapp.com',
        signIn: '/login',
        createAccount: '/users',
        getCards: '/cards',
        cardsCreate: '/cards',
    }
}

export default config;